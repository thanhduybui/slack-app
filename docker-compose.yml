services:
  postgres:
    image: postgres:16
    container_name: slack-postgres

    environment:
      POSTGRES_DB: slack_app
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5433:5432"

    volumes:
      - ./postgres-data:/var/lib/postgresql/data

    restart: unless-stopped